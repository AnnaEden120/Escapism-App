import React from "react";


function MovieList(){
const movies=['1','2','3']

 return( 
 <div className="movie">
   {movies.map(movie=>(

   ))}
 </div>
      )
}

export default MovieList;